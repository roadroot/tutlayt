name: dummy_app
description: Just a dummy project to test the generation.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-257.0.dev <4.0.0'

dependencies:
  graphql_decorator:
    path: ../graphql_decorator

dev_dependencies:
  build_runner: ^2.3.3
  graphql_generator:
    path: ../graphql_generator

